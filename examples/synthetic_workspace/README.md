# Synthetic workspace

This directory is fabricated test input. It contains only placeholder files
needed to exercise the public project adapters and does not represent a real
FLAC3D model, experiment, Zotero library, or research result.

Run from the repository root:

```powershell
python mining_kernel.py --workspace examples/synthetic_workspace discover
python mining_kernel.py --workspace examples/synthetic_workspace validate
```
